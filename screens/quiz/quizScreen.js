import React from 'react';

import {
    View,
    Text
} from 'react-native'

const quizScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Malayalam Quizzes</Text>
        </View>
    );
};

export default quizScreen;